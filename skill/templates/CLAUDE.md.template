# CLAUDE.md — Mycelium Living Repository

This repository is a **mycelium-enabled living repository**. It carries its own memory and grows smarter over time through structured traces of every action.

## Quick Orientation

1. **Read `.living/` first** — This is the repository's accumulated intelligence:
   - `.living/decisions.md` — Why past choices were made
   - `.living/learnings.md` — Gotchas, surprises, and insights from previous work
   - `.living/conventions.md` — Repo-specific convention overrides
2. **Read `ENVIRONMENTS_INSTALLATIONS.md`** — Environment setup, dependencies, and installation gotchas
3. **Read the relevant manifest** — Each top-level directory has a descriptive manifest (`ANALYSIS_MANIFEST.md`, `DATA_MANIFEST.md`, `ALGORITHM_MANIFEST.md`, `REFERENCE_MANIFEST.md`)

## Installed Skill Packs

<!-- Updated by install_domain_skill.py -->
<!-- Check .living/skills/ACTIVE_SKILLS.yaml for the full list -->

### Core (auto-installed)

- **robust-analysis** — Defensive execution practices: strict error handling, data validation checks, parameter sensitivity sweeps, null hypothesis testing, adversarial self-challenge. See `.living/skills/robust-analysis/analysis-conventions.md` for the entry point.
- **report-generator** — Structured LaTeX PDF report generation. See `.living/skills/report-generator/analysis-conventions.md` for the workflow.

### Domain (opt-in)

No domain skills installed yet. Install with mycelium's `install-skill` mode (e.g., "install bioinformatics skill").

## Repository Structure

```
├── algorithms/    — Reusable computational methods (see ALGORITHM_MANIFEST.md)
├── analysis/      — Analytical work (see ANALYSIS_MANIFEST.md)
├── data/          — Data assets: raw (immutable), processed, metadata (see DATA_MANIFEST.md)
├── reference_material/ — External references (see REFERENCE_MANIFEST.md)
├── todo/          — Future work items (see todo/TODOLIST.md)
└── .living/       — Repository memory layer
```

## Workflow

### Before Starting Work

1. Read `.living/learnings.md` for known gotchas relevant to your task
2. Read `.living/decisions.md` for past reasoning that may inform your approach
3. Read the manifest for the area you'll be working in (e.g., `ANALYSIS_MANIFEST.md`)
4. Check `.living/conventions.md` for any repo-specific overrides
5. If a domain skill is active, read its conventions in `.living/skills/[domain]/`

### While Working

- Follow analysis conventions: every analysis gets its own folder with UPPER_SNAKE_CASE.md documentation, scripts, outputs, reports
- Follow statistical conventions: report effect sizes, confidence intervals, document assumptions
- **Follow robust-analysis conventions** (`.living/skills/robust-analysis/`): fail loudly on unexpected data, assert shapes/types/ranges, log row counts at every step, run sensitivity analyses for every decision, test null hypotheses via permutation/bootstrap
- **Do not subset data** without explicit user confirmation and justification
- Use marimo for exploration, Python scripts for reproducible pipelines
- Every analysis must have a `run.sh` or `run.py` that reproduces final outputs

### After Every Significant Action (Post-Action Hook Protocol)

**This is critical.** After completing any significant step:

1. **Update manifests**: Update the relevant manifest (`ANALYSIS_MANIFEST.md`, `DATA_MANIFEST.md`, etc.) with new/changed entries
2. **Update documentation**: Update or create the UPPER_SNAKE_CASE.md file in the affected subfolder
3. **Log decisions**: If a non-obvious choice was made, append to `.living/decisions.md`
4. **Log learnings**: If something unexpected happened, append to `.living/learnings.md`
5. **Log todos**: If future work is identified, add items to `todo/TODOLIST.md` (create a detailed `todo/[item].md` writeup for complex items)
6. **Validate**: Run `validate_structure.py` to confirm repo structure is correct
7. **Skill feedback**: Note whether domain skill conventions were helpful or had gaps

### Automated Enforcement

If Claude Code hooks are installed (see mycelium `skill/hooks/`), the Stop hook will block session end if `.living/` was not updated after significant work. This ensures the post-action protocol is never silently skipped, even in long sessions where earlier context is compacted.

### Subagent-Driven Workflows

When using subagent-driven development (main context coordinates, subagents implement):
- After all subagent batches complete, dispatch a **crystallization subagent** to log decisions and learnings to `.living/`
- The Stop hook catches sessions where this step is forgotten
- Subagents themselves do not need mycelium awareness — only the main context manages the living layer

## Data Conventions

- `data/raw/` is **IMMUTABLE** — never modify original files
- Every dataset has metadata in `data/metadata/[dataset-name]/`
- Every dataset has a manifest entry in `data/DATA_MANIFEST.md`
- Large files are gitignored with download instructions documented

## Key Files

| File | Purpose |
|------|---------|
| `ENVIRONMENTS_INSTALLATIONS.md` | How to set up the environment |
| `.living/decisions.md` | Why choices were made |
| `.living/learnings.md` | Accumulated insights and gotchas |
| `.living/conventions.md` | Repo-specific convention overrides |
| `todo/TODOLIST.md` | Master list of future work items |
| `*/*_MANIFEST.md` | Registry of contents in each directory |
